Author: Mir Abbas


1)   Install node.js

2)   Write "npm install" in the terminal (staying in the folder , in the terminal)

In the terminal type : DEBUG=myapp ./bin/www
or simply ./bin/www

 
3)   The server will be running at localhost:3000/
